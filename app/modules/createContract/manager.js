const db = require('../../../database/models/index');
// const user = db.User;
export default class Manager {
    createXrc20Token = async (requestData) => {
        // API business logic

        return {};

    }
}
